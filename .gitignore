# Secrets - never commit (.env.example is the tracked template)
.env
.env.local
.env.*.local

# Dependencies
node_modules/
.pnpm-store/

# pnpm is the only package manager here - never commit another lockfile
package-lock.json
yarn.lock
bun.lockb

# Build output
dist/
build/

# Logs
*.log
npm-debug.log*
runs/
